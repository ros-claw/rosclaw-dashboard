# ROSClaw Web Dashboard

Next.js frontend for ROSClaw Dashboard.

## Setup

```bash
cd apps/web
pnpm install
```

## Run

```bash
pnpm dev
```

The dev server runs on `http://localhost:3000` and proxies API requests to `http://127.0.0.1:8000`.

## Build

```bash
pnpm build
```
